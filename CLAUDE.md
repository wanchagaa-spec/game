# CLAUDE.md — เกมไข่ทหาร (Roblox)

## ภาษา
ตอบเป็น **ภาษาไทย** เสมอ ทั้งคำอธิบาย สรุป และคำถามกลับ
(comment ในโค้ดใช้ไทยได้ ชื่อตัวแปร/ฟังก์ชันใช้อังกฤษตามมาตรฐาน Luau)
สไตล์: ตรงประเด็น กระชับ ไม่ต้องเกริ่นยาว

## โปรเจกต์นี้คืออะไร
เกม Roblox แนวผสม: **ฟาร์มสะสม + auto-battle + แย่งไข่แบบ PvP อ่อน ๆ**
1 เซิร์ฟเวอร์มีผู้เล่น 6 คน ใช้แมพร่วมกัน แต่กำแพงเป็นของแต่ละคน (คอก 6 แปลง = 6 คนพอดี)

Core loop:
ฆ่าบอส → แย่งไข่ → ฟักได้ตัวแม่ (สุ่มตัวละคร + น้ำหนัก) → วางแม่ในคอก →
แม่ผลิตลูก + ผลิตเงิน → ปล่อยลูกต่อเนื่องไปพังกำแพงด่านถัดไป →
ปลดล็อกพื้นที่บอสด่านใหม่ → ไข่ดีขึ้น → วนซ้ำ

## กลไกเกมหลัก
รายละเอียดเต็ม + ที่มาของทุกตัวเลข อยู่ใน `docs/data-schema.md` ตรงนี้เป็นฉบับย่อ

### ตัวแม่ (Mother)
- ได้จากการฟักไข่ที่แย่งมาจากรังบอส **เก็บรายตัว**
- มี **ตัวละคร** (กำหนดคลาส → ตัวคูณ damage/HP) และ **น้ำหนัก** (สุ่ม 7 ชั้น 100 kg – 100,000,000 kg)
- สุ่ม 2 ขั้นเสมอ: เลือก tier ก่อน แล้วค่อยสุ่มน้ำหนักในช่วงของ tier นั้น
  **ห้ามสุ่ม uniform ทั้งช่วงรวดเดียว** (จะทำให้ตัวเล็กแทบไม่มีวันออก)
- น้ำหนักแม่เป็น **จำนวนเต็มเสมอ** เพราะเป็นส่วนหนึ่งของ stack key
- อยู่ได้ 2 ที่: **คอก** (ผลิตลูก + ผลิตเงิน) กับ **กระเป๋า** (ไม่ผลิตอะไรเลย จุ 100 ตัว)
- ขายทิ้งได้ ได้เงินเท่ารายได้ 30 นาทีของแม่ตัวนั้น

### ตัวละคร 12 ตัว แบ่ง 5 คลาส
| คลาส | ตัวคูณ damage/HP | ตัวละคร | ออกจากไข่ |
|---|---|---|---|
| SS | **×432** | องค์ยูไล | **ไข่ตำนาน (Robux) เท่านั้น** |
| S | **×216** | พระแม่กวนอิม · เง็กเซียนฮ่องเต้ | บอสด่าน 7+ (1% → 20%) · ตำนาน 18% |
| A | **×36** | พระถังซัมจั๋ง · ซุนหงอคง | บอสด่าน 3+ (0.1% → 50%) · ตำนาน 45% |
| B | **×6** | ตือโป๊ยก่าย · ซัวเจ๋ง · ม้าขาวมังกร | ทุกด่าน (10% → 20%) · ตำนาน 35% |
| C | ×1 | ลิง · หมู · ม้า · ปลา | ทุกด่าน (90% → 10%) |

**ไข่จากบอสออกได้ C ถึง S · SS ออกจากไข่ตำนานเท่านั้น** (`validate()` บังคับกฎนี้)

⚠️ **คลาสไล่ ×6 ต่อขั้น เพราะคลาสคือแกนเดียวที่ไต่ตามด่าน** (ตารางน้ำหนักเหมือนกันทุกด่าน)
⚠️⚠️ **SS หยุดที่ ×2 เหนือ S โดยตั้งใจ ห้ามแก้เป็น 1,296** — SS ออกจากไข่ Robux เท่านั้น
ถ้าไล่ ×6 ต่อ คนจ่ายเงินจะแรงกว่าคนไม่จ่าย 1,296 เท่า = pay-to-win เต็มรูปแบบ
`validate()` บังคับ `SS ÷ S ≤ 2.5` — เกินแล้วเซิร์ฟไม่บูต

คลาสกับน้ำหนักตอนนี้สูสีกัน: น้ำหนักให้ ×1,000 ตลอดเกม คลาสให้ ×216 (ฟรี)
แม่ 100 kg คลาส SS (4,320) แพ้แม่ 100,000 kg คลาส A (11,400) — สองแกนตัดกันจริง

### ตัวลูก (Child)
- แม่**ในคอก**ผลิตลูกอัตโนมัติ ลูกหนัก **1% ของแม่** และได้ตัวละคร+สถานะของแม่
- **อัตราผลิตแปรตามน้ำหนักแม่**: `ฐาน × (น้ำหนัก ÷ 100)^0.25`
  แม่ 100 kg = 1 ตัว/นาที · 10,000 kg = 3.2 · 1M kg = 10 · 100M kg = 31.6
  ใช้ 0.25 ไม่ใช่ 0.5 เพราะน้ำหนักมีผลอยู่แล้ว 2 ทาง (เงิน √ · damage ต่อตัว √)
  ถ้าใช้ √ ด้วย damage รวม/วันจะแปรผัน**ตรง**กับน้ำหนัก ช่องว่างจะเป็น 1 ล้านเท่า
- ออฟไลน์ = 1/10 ของออนไลน์ · สะสมออฟไลน์ได้สูงสุด **8 ชั่วโมง**
- คำนวณจาก **timestamp** ไม่ใช่ loop นับสด
- **คลังจุได้ 500 ตัวต่อกอง** (ตายตัว ไม่มีระบบอัป) เต็มแล้วแม่ตัวนั้นหยุดผลิต + แจ้งเตือน
  (500 ตัว ≈ 8 ชม. ของแม่ 100 kg = ตรงกับ cap ออฟไลน์ที่ใช้อยู่แล้ว)
- **ลูกไม่แสดงในแมพจนกว่าจะปล่อยออกรบ** ในคลังเป็นแค่ตัวเลขนับจำนวน
- ลูกที่ตัวละคร + น้ำหนัก + ชุดสถานะเหมือนกัน → **รวมเป็นกองเดียว เก็บแค่ count**

### สถานะ (Status)
- สถานะติดที่ **ตัวแม่เท่านั้น** ลูกไม่มีสถานะของตัวเอง
- ลูกที่ผลิตออกมา **คัดลอกชุดสถานะของแม่ ณ เวลาที่ผลิต** (snapshot)
- แม่ได้สถานะเพิ่มทีหลัง → **ลูกที่ผลิตไปแล้วไม่อัปเดตย้อนหลัง** เข้ากองใหม่แทน
  → แม่ตัวเดียวมีลูกได้หลายกองตามประวัติสถานะ
- สถานะ 1 ตัวให้บัฟได้ 4 อย่าง: **คูณ damage · คูณเงิน · คูณความเร็วผลิต · เพิ่มน้ำหนักลูก**
  (ติดหลายสถานะ = ตัวคูณคูณสะสม ส่วนโบนัสน้ำหนักลูกบวกกัน มีเพดานไม่ให้ลูกหนักเกินแม่)
- Phase 1-5 ยังไม่เปิดใช้ (`enabled = false`) แต่ schema + ตัวคูณรองรับไว้แล้ว
  ตัวอย่างที่ตั้งไว้: gold ×2 damage/เงิน · ×1.5 ผลิต · ลูก 1% → 2% · silver ×1.5/×1.5/×1.2
- ⚠️ บัฟที่คูณ damage คุ้มกว่าเพิ่มน้ำหนักลูกมาก เพราะ damage ใช้ sqrt
  (น้ำหนักลูก ×2 ได้ damage แค่ ×1.41 แต่ `damageMultiplier = 2` ได้ ×2 เต็ม)

### Stack key
รูปแบบ `"<ตัวละคร>|<น้ำหนักแม่>|<สถานะเรียงแล้ว>"` เช่น `"wukong|1500|gold,silver"`
- ใช้น้ำหนัก **แม่** (จำนวนเต็ม) ไม่ใช่น้ำหนักลูก (ทศนิยม → key เพี้ยน)
- **เรียงสถานะเสมอ** ไม่งั้น `{gold,silver}` กับ `{silver,gold}` กลายเป็นคนละกอง
- ใช้ `Config.makeStackKey()` / `Config.parseStackKey()` เท่านั้น **ห้ามต่อ string เอง**

### uid ของตัวแม่ — เป็น global
รูปแบบ `"<UserId ผู้ฟักคนแรก>-<เลขนับ>"` เช่น `"1234567-42"` เป็น **string ไม่ใช่ number**
- ต้อง global เพราะมีแผนจะทำ **เทรดแม่ระหว่างผู้เล่น** — ถ้าไม่ซ้ำแค่ในคนเดียว
  พอแม่ย้ายเจ้าของ uid จะไปชนกับแม่ของคนรับ แล้วทีมที่จัดไว้จะชี้ผิดตัว
- `UserId` ในนี้คือผู้ฟักคนแรก **ห้ามเปลี่ยนตอนเทรด**
- ใช้ `Config.makeUid()` / `Config.parseUid()` เท่านั้น

### แหล่งที่มาของไข่ — สองทาง แยกกันเด็ดขาด
**เงินในเกมซื้อไข่ไม่ได้เลย** ใช้ได้แค่ อาวุธ · อัปเกรดอาวุธ · อัปเกรดคอก
(`EggType` จึงไม่มีฟิลด์ `price` โดยตั้งใจ ทุกไข่มี `source` แทน)
- **ไข่ปกติ** (`source = "boss"`) — แย่งจากรังบอสเท่านั้น
  **บอสแต่ละด่านวางไข่คนละชนิด** `egg_stage1` … `egg_stage9` (`Config.getBossEggId(stage)`)
  `validate()` บังคับว่าทุกด่านต้องมีไข่ของตัวเองที่เปิดใช้อยู่ ไม่งั้นด่านนั้นตันถาวร
  **ตารางสุ่มน้ำหนักใช้ชุดเดียวกันทุกด่าน** — ด่านสูงคุ้มกว่าที่ *โอกาสได้คลาสดี* ไม่ใช่ตัวหนัก

  | ด่าน | C | B | A | S |
  |---:|---:|---:|---:|---:|
  | 1 | 90% | 10% | — | — |
  | 2 | 80% | 20% | — | — |
  | 3 | 70% | 29.9% | 0.1% | — |
  | 4 | 60% | 39% | 1% | — |
  | 5 | 50% | 45% | 5% | — |
  | 6 | 40% | 50% | 10% | — |
  | 7 | 30% | 45% | 24% | 1% |
  | 8 | 20% | 30% | 40% | 10% |
  | 9 | 10% | 20% | 50% | 20% |

  ⚠️ `egg_common` / `egg_rare` **ปิดแล้ว** (`enabled = false`) ห้ามลบ ห้าม reuse
- ⚠️ **ลำดับการสุ่ม: น้ำหนักมาก่อน และมาตั้งแต่ตอนบอสวางไข่**
  บอสเกิด → สุ่มน้ำหนักไข่ทั้ง 5 ฟองทันที → **ขนาดโมเดลไข่แปรตามน้ำหนัก** (ไข่ใหญ่ = หนัก)
  ผู้เล่นเห็นพร้อมกันว่าฟองไหนคุ้มค่าแย่ง → แย่งไข่กลายเป็นเกมจริง ไม่ใช่รีบกดสุ่ม
  ตอนฟักถึงค่อยสุ่ม **ตัวละคร** (`Config.rollCharacter`)
  → ผลต่อ schema: ไข่ต้องเก็บ **รายฟองพร้อมน้ำหนัก** (`heldEggs` เป็นอาเรย์ ไม่ใช่ตัวนับ
    · `hatching` มีฟิลด์ `weight`)
- **ไข่ตำนาน** (`source = "robux"`) — ซื้อด้วย **Robux** ผ่าน Developer Product เท่านั้น
  **รับประกัน tier 3 ขึ้นไป** (10,000 kg+) โดยสุ่มตามตารางปกติก่อน ถ้าได้ต่ำกว่าค่อยดันขึ้น
  → จ่ายแล้วไม่มีทางเสียใจ แต่ยังลุ้น tier 7 (1 ในล้าน) ได้ตามอัตราเดิม
  ⚠️ **ห้ามเก็บราคา Robux ใน Config** ราคาจริงอยู่ใน Creator Dashboard ที่เดียว
  client ดึงราคามาโชว์ด้วย `MarketplaceService:GetProductInfo()`
  ⚠️ `ProcessReceipt` ต้อง idempotent ด้วย `PurchaseId` และ
  **ห้ามคืน `PurchaseGranted` ก่อนเซฟ DataStore สำเร็จ** (ดู `docs/data-schema.md` §8.7)

### เงิน — สามบ่อ
- **แม่ในคอก** `coins/นาที = 1 × (น้ำหนักแม่/100)^0.5 × 10^(wallProgress-1)`
- **ฆ่าทหารฝ่ายรับ** `100 × 2^(ด่าน-1)` ต่อตัว → เงินรวมทั้งด่านโต **×20 ต่อด่าน**
  ⚠️ ทหารตายแล้วไม่เกิดใหม่ + `stageProgress` เซฟถาวร → **1 ตัวจ่ายครั้งเดียวตลอดกาล**
  ห้ามจ่ายซ้ำตอนโหลดข้อมูลกลับมา และห้ามจ่ายจากด่านที่พังไปแล้ว
- **ฆ่าบอส** `10,000 × 10^(ด่าน-1)` ต่อครั้ง (ใช้ ×10 เพราะบอสฆ่าซ้ำได้ จำนวนครั้งไม่โตตามด่าน)
  **แบ่งเท่ากันทุกคนที่ร่วมตี** (`Config.getBossKillShare`) ไม่แบ่งตามสัดส่วน damage
  เพราะตีบอสใช้เวลาไม่นานและเป็นแค่กิมมิค · นับว่าร่วมตีเมื่อทำ damage ≥ 1
  ยอมรับแล้วว่าคนที่ตีครั้งเดียวตอนใกล้ตายก็ได้เต็ม — แลกกับที่ผู้เล่นใหม่ไล่ทันได้
- ⚠️ **server คำนวณและมอบทั้งหมด ห้าม client แจ้งยอด** (รวมถึงรายชื่อคนที่ร่วมตีบอส)
- **เงินที่เคยล้น 10–299 เท่า ถูกดูดด้วย upgrade ตัวคูณ damage แล้ว** เหลือ 1.6–2.5 เท่า

### ซื้อความเร็ววิ่ง — บ่อเงินบ่อที่สอง
`Config.Balance.SpeedUpgrade` · `speedLevel` ใน PlayerData · รายละเอียดเต็มใน `docs/data-schema.md` §8.8

- ความเร็วฐาน 32 · **5 ขั้น** ราคา 10K → ×10 ทุกขั้น (10K · 100K · 1M · 10M · 100M รวม 111.11M)
- ตัวคูณ **ถดถอย** สูงสุด ×4 → `1 + 3 × (ขั้น ÷ 5)^0.75` · ขั้น 1 เร็วขึ้น 90% · ขั้น 5 แค่ 13%
  (`CURVE_EXPONENT` ปรับได้ใน Config ไม่ hardcode)
- **เป็นของบัญชีผู้เล่น ไม่ใช่ของตัวละคร** ตายแล้วเกิดใหม่ได้ความเร็วเดิมกลับมา
- ⚠️⚠️ **5 ขั้น ไม่ใช่ 10 — ตัดสินถาวร ห้ามขยาย**
  ราคาไล่ ×10 ถ้าทำ 10 ขั้น ขั้นสุดท้ายแพงกว่าขั้นแรก **หนึ่งพันล้านเท่า** แต่ตัวคูณชนเพดาน ×4 แล้ว
  จึงให้ความเร็วเพิ่มแค่ 3–4% = ขั้นที่ไม่มีใครซื้อ **ความผิดพลาดแบบเดียวกับคอก Lv11–15 ที่ตัดทิ้งไปแล้ว**
- ⚠️ **เพดานความเร็ว 200** (`SPEED_CEILING`) เกินแล้วทะลุของทั้งแมพ แก้ด้วยความหนากำแพงไม่ได้
- 🔴 **สองบ่อเงินไม่ได้แข่งกันจริง — ยอมรับแล้ว** `WalkSpeed` ไม่อยู่ในสมการ damage/วินาที เลย
  (ลูกเกิดที่แท่นปล่อยต้นเลน ไม่ได้เดินมาจากคอก) → "เร่ง damage ก่อน" เป็นคำตอบเดียวเสมอ
  ที่ยอมรับได้เพราะ **damage มีเพดานต่อด่าน** ซื้อครบแล้วเงินไม่มีที่ไป ความเร็วจึงเป็น**บ่อดูดส่วนเกิน**
  ⚠️ อยากให้เป็นการเลือกจริงต้องทำให้ความเร็วมีผลต่อ throughput ของไข่ (ยังไม่ได้ทำ)

### แมพ 9 ด่าน และกำแพง
- แมพเป็นเส้นตรง 9 ด่าน **กำแพงเป็นของแต่ละคน** พังแล้วเปิดถาวรสำหรับคนนั้น
  (`wallProgress` เก็บใน PlayerData ไม่ใช่ per-server)
- **ด่าน 1 ไม่มีกำแพงและไม่มีทหารฝ่ายรับ** ผู้เล่นใหม่เดินไปสู้บอสตัวเล็กเอาไข่ได้เลย
  (แก้ปัญหาไก่กับไข่: ต้องมีแม่ถึงจะมีกองทัพ ต้องมีไข่ถึงจะมีแม่) `validate()` บังคับข้อนี้
- **พื้นที่บอสใช้ร่วมกันทั้ง 6 คน** แต่เข้าได้เฉพาะคนที่พังกำแพงถึงด่านนั้น
- ทหารฝ่ายรับด่าน N = 10 × 10^(N-1) ตัว (ด่าน 9 = 1,000,000,000 ตัว) **ตายแล้วไม่เกิดใหม่**
- ⚠️ **ห้าม spawn ทหารฝ่ายรับเป็น Model จริง** เก็บเป็น "HP รวม" ค่าเดียว
  แล้วแสดงโมเดลประกอบ 20–50 ตัวที่ค่อย ๆ หายตามสัดส่วน HP
- ลำดับการตี: **ทหารฝ่ายรับหมดก่อน แล้วค่อยตีกำแพง**
- **ตัวที่ตายหายถาวร** ทั้งแม่และลูก · แม่ตาย = เสียเครื่องผลิตถาวร

### แมพ — สามโซนเรียงซ้ายไปขวา
📄 ผังเต็ม + พิกัดจริง + กฎ client/server อยู่ใน `docs/map-layout.md`

**แมพเป็นลานหญ้าเปิดโล่งบนแท่นลอย ไม่มีกำแพงล้อมรอบแมพ**
ที่เดียวที่มีกำแพงข้างคือเลนรบ · ขอบแมพกันด้วย **กำแพงใส** (`Transparency = 1`, `CanCollide = true`)

```
แผงร้าน  →  ลานคอก (6 แปลง 2 แถว + ทางเดินกลาง)  →  เลนรบ (9 ด่าน)
  ทั้งหมดอยู่บนแท่นลอยผืนเดียว 2,002 × 390 studs
```

- **แมพสร้างจากสคริปต์เท่านั้น** (`src/server/MapBuilder.lua`) **ห้ามปั้นโมเดลใน Studio แล้ว export**
  Rojo sync ได้แค่ไฟล์สคริปต์ · ของที่ปั้นมือแก้ผ่าน repo ไม่ได้และปรับตัวเลขทีต้องปั้นใหม่
- ⚠️ **`Config.MapDimensions` เป็นแหล่งความจริงแหล่งเดียวของรูปทรง — มีชื่อเดียว**
  (`Config.Map` ที่เคยเป็น alias **ยุบทิ้งแล้ว**)
  ผังบนพื้นใช้ `vec2(กว้าง, ลึก)` ส่วนขนาด Part จริงใช้ `vec3`
- **ทุกพิกัดมาจาก `Config.get*()`** ห้าม hardcode ตัวเลขใน MapBuilder
- **ลานคอก** — 6 แปลง 2 แถว หันหน้าเข้าหากัน แปลงละ **80 × 80** · แปลงละคน จองตอนเข้า คืนตอนออก
  พื้นในคอก**เป็นหญ้าทั้งผืน ไม่มีช่องตาราง** แม่เดินอิสระ ไข่วางตรงไหนก็ได้ · เห็นฟาร์มคนอื่นได้
  **ทางเดินกลางก็เป็นหญ้า** กลืนกับพื้นรอบข้าง ไม่มีเส้นแบ่ง
  ⚠️ **ทางเดินกลาง (90) ต้องกว้างไม่น้อยกว่าเลนรบ (60)** ไม่งั้นปากทางเข้าเลนเป็นคอขวด · `validate()` บังคับ
- **รั้วคอก** — รั้วไม้เตี้ย สูง 3 หนา 1 · เสาทุก 8 studs + คาน 2 ชั้น
  ⚠️ **`CanCollide = false` ทะลุได้ทั้งคนและแม่** จึง**ยกเว้นจากกฎความหนา-ความเร็ว**
  (กฎนั้นมีไว้กับกำแพงที่ต้อง *หยุด* ผู้เล่นเท่านั้น)
  **แม่ยังเดินแค่ในคอก** เพราะขอบเขตสุ่มเดินอ่านจาก**พิกัดคอก ไม่ใช่จากรั้ว**
  **ประตู** = เว้นช่องกลางรั้ว 8 studs ด้านที่หันเข้าทางเดิน (ทั้ง 6 คอก) · ไม่มีบานเปิด/ปิด
  **ป้ายชื่อ** ปักข้างประตู ไม่ใช่กลางประตู (กันเดินชน)
- **เลนรบ** — เลนเดียวใช้ร่วมกัน กว้าง 60 · **มีกำแพงสูง 40 สองข้างตลอดเลน (ฝั่ง server)**
  จุดปล่อยทหารอยู่ต้นเลน **ทหารโผล่ที่นั่นเลย ไม่เดินมาจากคอก**
  ด่าน N กินช่วงยาว `LANE_LENGTH_PER_STAGE` · **กำแพงอยู่ต้นช่วง รังบอสอยู่ท้ายช่วง (หลังกำแพง)**
  ⚠️ ห้องบอส (80) กว้างกว่าเลน (60) → **กำแพงข้างเลนต้องผายออกตรงห้องบอส**
  ใช้ `Config.getLaneHalfWidthAt(x)` ห้ามลากเป็นเส้นตรงเส้นเดียว
- **ร้านค้า** — **แผงเล็ก 2 แผง (12 × 12) ที่ขอบลาน ไม่ใช่อาคาร** เป็นแค่ฉาก ของจริงคือ UI
  ⚠️ **ไม่มีแท่นวาปแล้ว** เดินไปเองทุกที่ · และ**ไม่มีวาปไปรังบอสด้วย** (วาปได้ = การแย่งไข่หมดความหมาย)
  ⚠️ **อัปเกรดมีปุ่มติดตัว 2 ปุ่ม (damage 72 ขั้น · ความเร็ว 5 ขั้น) ไม่ต้องเดินมาร้าน**
- **ผู้เล่นวิ่ง 32 studs/วิ** (×2 จากค่าปกติ) ตั้งที่ `StarterPlayer.CharacterWalkSpeed`
  ⚠️ **กระโดดสูง 7.2** — ตั้ง `CharacterUseJumpPower = false` + `CharacterJumpHeight = 7.2`
  Roblox มีสองโหมด ถ้า `UseJumpPower` ยังเป็น true ค่า `JumpHeight` จะ**ถูกเพิกเฉยทั้งค่า**
  แล้วความสูงจริงมาจาก `JumpPower` แทน (50 → 6.37) · `Main.server.lua` เช็คทั้งโหมดและตัวเลขตอนบูต
  **ซื้อเพิ่มได้ถึง ×4 = 128** (ดูหัวข้อ "ซื้อความเร็ว" ข้างล่าง)
  **เกิดใกล้คอกตัวเอง** — `SpawnLocation` หนึ่งจุดต่อคอก + `player.RespawnLocation`
- ⚠️⚠️ **ความหนากำแพงทุกชนิดผูกกับความเร็วสูงสุด** `≥ ความเร็วสูงสุด ÷ 60 × 2` = 4.27
  กำแพงกั้นด่าน · กำแพงข้างเลน · กำแพงใสขอบแมพ = **5 ทั้งหมด** (รั้วคอกยกเว้น — ไม่ชน)
  บางกว่านั้น = วิ่งข้ามไปทั้งชิ้นระหว่างสองเฟรมโดยไม่ชน · `validate()` บังคับ
- ⚠️ **ขอบแมพใช้กำแพงใส ไม่ใช้ "ตกแล้วเกิดใหม่"** — ถูกดีดกลับตอนกำลังฟาร์มน่ารำคาญมาก
  `validate()` บังคับ `Boundary.Height > Player.JumpHeight`
  ⚠️ **`Boundary.Margin` = แถบหญ้าที่เห็นแต่เดินไม่ถึง** · พื้นลานโตตามค่านี้เอง
  **ไม่ใช่การหดกำแพงเข้ามา** — เคยพลาดแบบนั้นแล้วกำแพงกินเข้าไปในคอก 30 studs
  `validate()` บังคับว่ากำแพงใสต้องอยู่นอกทุกคอกและทุกแผงร้าน

### ⚠️ อะไรวาด client อะไรอยู่ server
| server (ทุกคนเห็นเหมือนกัน) | client (เห็นเฉพาะของตัวเอง) |
|---|---|
| ลานคอก · เลนรบ · รังบอส · ร้านค้า | **กำแพง 9 ด่าน** |
| ตัวแม่ในคอก · ไข่ที่กำลังฟัก | **ทหารฝ่ายรับ** |
| บอส · ไข่ในรัง | **กองทัพของผู้เล่นเอง** |

**กำแพงต้องวาดฝั่ง client** เพราะทุกคนใช้เลนเส้นเดียวกันแต่พังมาถึงคนละด่าน
สร้างบน server จะมีชุดเดียวให้ทุกคน แยกไม่ได้ว่าใครผ่านแล้ว
→ ผลพลอยได้ที่ตั้งใจ: **การชนแยกกันเองฟรี ๆ** (Part ฝั่ง client ชนเฉพาะเจ้าของเครื่อง)
→ ผลข้างเคียงที่**ยอมรับแล้ว ไม่ต้องแก้**: เห็นคนอื่นเดินทะลุกำแพงที่ตัวเองยังพังไม่ได้

### ⚠️ วางของบนพื้นคอก — ใช้ `Config.getPenRestingY()` เท่านั้น
- **ผิวบนของพื้นคอก = `Pen.FloorThickness` เต็ม ๆ** (แผ่นพื้นกินช่วง `0 .. FloorThickness`)
- `Y ของจุดศูนย์กลาง = getPenRestingY(ครึ่งความสูงจริงของชิ้นนั้น)`
- ⚠️⚠️ **`Shape = Ball` ต้องตั้งขนาดเท่ากันทั้งสามแกน**
  Roblox ไม่รับประกันว่าลูกบอลที่ขนาดไม่เท่ากันจะวาดเป็นทรงกลม (`min`) หรือทรงรี
  เคยตั้ง `(3, 3.8, 3)` → เลข 3.8 ไม่มีผลต่อภาพ แต่หลอกให้โค้ดคิดว่ารัศมี 1.9 ทั้งที่จริง 1.5 → ไข่ลอย
  ใช้ `Config.getBallRadius()` หารัศมี **ห้ามใช้ `Size.Y / 2` กับทรงกลม**
- ⚠️⚠️ **แผ่นพื้นคอกต้องบางจนมองไม่เห็น** (`Pen.FloorThickness` = 0.05)
  เพราะแผ่นนี้ **`CanCollide = false` และสีเกือบเท่าหญ้า** → ผู้เล่นเดินบนหญ้าที่ `Y = 0`
  ของที่วางบนผิวแผ่นจึงดู **ลอยเหนือพื้นเท่ากับความหนาแผ่น**
  เคยตั้ง 0.3 แล้วไข่ (ทรงกลม แตะพื้นจุดเดียว) ดูลอยชัด ส่วนแม่ไม่เห็นเพราะก้นแบน
- **ห้ามคำนวณ Y เองในแต่ละที่** — เคยแยกกันสองที่แล้วหลุดจากกันมาแล้ว

### ⚠️ สัตว์ที่เดินได้ในคอก
- **ห้ามใช้ `Humanoid`** — คอกเต็ม 19 ตัว × 6 คน = 114 Humanoid หนักเกินไป
- ใช้ **CFrame lerp** สุ่มจุดหมายในคอกแล้วเดินไปหา · **ลูปเดียวคุมทุกตัว**
- **ห้ามเซฟตำแหน่งลง DataStore** สุ่มใหม่ทุกครั้งที่เข้าเกม
  (ตำแหน่งไม่มีความหมายเชิงเกม และเซฟแม่ทุกตัว + ไข่หมื่นฟองจะกิน DataStore ฟรี ๆ)
- **โมเดล mesh ต่อตัวละคร** — `Character.modelAssetId` (optional) = id ของ **Model asset** ที่ publish ขึ้น Roblox แล้ว
  `PenService` โหลดด้วย `InsertService:LoadAsset` ครั้งเดียวแล้ว clone · ไม่มี/โหลดไม่ได้ = กล่องสีตามคลาสเหมือนเดิม
  ⚠️ **ไฟล์ .glb/.fbx ใส่ใน repo ตรง ๆ ไม่ได้** ต้อง Import 3D ใน Studio → Save to Roblox ด้วยบัญชี/กลุ่ม**เดียวกับเจ้าของเกม**
  (LoadAsset โหลดของคนอื่นไม่ได้) · Humanoid ที่ติดมาถูกถอดทิ้งอัตโนมัติ · ทุกชิ้นถูกบังคับ Anchored + CanCollide = false
  ⚠️ **เลขต้องเป็น Model (AssetTypeId 10) ไม่ใช่ Mesh (4)** — เลข Mesh ทำ LoadAsset ค้างเงียบ ๆ ไม่ error (เคยเจอจริง)
  · โหลดเบื้องหลังเสมอ `refreshMothers` ห้าม yield รอ LoadAsset
  **อนิเมชัน** `Character.animationIds = { walk, idle, sit, punch }` (ใส่เท่าที่มี · ต้องมี `modelAssetId` ด้วย) เล่นผ่าน
  `AnimationController` + `Animator` ฝั่ง server (**ไม่ใช่ Humanoid** · เก็บ AnimationController ไว้ ไม่ถอด)
  เดินตอนเคลื่อนที่ · หยุดพักวนท่าพักที่มีตามลำดับ ยืน → นั่ง → ต่อย (ข้ามท่าที่ไม่มี)
  · ท่าต้อง publish แบบ **Loop** (ค่า Looped จาก server ไม่ส่งไป client)
  **ขนาด**: วัดความสูงต้นฉบับแล้วสเกลให้ tier 1 สูง `VisualScale.MOTHER_MESH_BASE_HEIGHT` (5 = เท่าคน)
  ไม่ใช้สเกลดิบ (ไฟล์แต่ละไฟล์ import มาไม่เท่ากัน) · **ก้าว**: ตัวใหญ่ s เท่า เดินเร็ว √s + อนิเมชันช้า √s
  → ระยะต่อก้าวโต s เท่าพอดีตัว (`MOTHER_MESH_WALK_SPEED` = ความเร็วที่ tier 1) · แม่ทุกตัวเว้นขอบคอก
  ครึ่งเส้นทแยงพื้นของตัวเอง กันตัวใหญ่ยื่นทะลุรั้ว
  ตอนนี้มีแค่ `monkey` = โมเดลลิง (`109724272624838`) + เดิน/ยืน/ต่อย · **ยังไม่ได้ทดสอบใน Studio**
  ⚠️ โมเดลกอริลลาเดิม**ไม่ได้ใช้แล้ว** แต่ยังอยู่ในบัญชี เผื่อเอาไปใส่ตัวละครอื่น: model `109867818523029` ·
  เดิน `110226605923724` · ยืน `83105969878758` · นั่ง `71506621911711`

### การรบ — ปล่อยทหารต่อเนื่องแบบ Age of War
⚠️ ระบบเดิม "กดส่งกองทัพทีเดียวแล้วรอผล" **ยกเลิกทั้งหมดแล้ว**

- มีจุดสปอนที่ต้นด่านของแต่ละคน ปล่อยลูกทีละตัวเรียงแถวเดินไปทางกำแพง
- **อัตราปล่อยเป็นเพดาน damage/วินาที** `Config.Balance.Combat.RELEASE_PER_SECOND`
  = `{1, 1, 1, 2, 3, 4, 6, 8, 10}` ตัว/วินาที ตามด่าน
- `damage/วินาที = min(อัตราผลิต, อัตราปล่อย) × damage ต่อตัว × ตัวคูณด่าน`
  ⚠️ **พอชนเพดานปล่อย upgrade อัตราผลิตหลุดออกจากสมการ damage ทันที**
  (ยังมีประโยชน์ เพราะเติมคลังเร็วขึ้น แต่ไม่ใช่ตัวคูณ damage อีกต่อไป)
  คอขวดอยู่ที่ "การผลิต" ด่าน 1–2 แล้วพลิกเป็น "การปล่อย" ตั้งแต่ด่าน 3 ถาวร
- **ปล่อยเฉพาะตอนออนไลน์** ออฟไลน์ไม่ปล่อย (แต่แม่ยังผลิตตามกติกาออฟไลน์เดิม)
- **ปุ่มอัญเชิญ (เปิด/ปิด)** ปิด = สะสมไว้ในคลัง · เปิด = ปล่อยต่อเนื่องอัตโนมัติ
  กลไกหลักของเกมคือสะสมกองใหญ่แล้วปล่อยรวดเดียวทะลุ ดีกว่าปล่อยทีละตัวแล้วถูกกินทีละตัว
- **auto-pause**: ปล่อยครบ 100 ตัวแล้ว HP ฝ่ายตรงข้ามไม่ลดเลย → หยุดปล่อยเอง + แจ้งเตือน
- **แสดงเฉพาะทหารของผู้เล่นคนนั้นเอง** cap 120 โมเดลพร้อมกัน ส่วนเกินรวมเป็นตัวเลข
- ⚠️ **ห้ามระบบ auto ปล่อยตัวแม่เด็ดขาด** auto ปล่อยได้เฉพาะตัวลูก
  แม่ต้องกดเลือกส่งเองทีละครั้ง + กล่องยืนยัน และ**เลือกได้เฉพาะแม่ในกระเป๋า**
  ⚠️ **แม่ในคอกไม่ขึ้นในกระเป๋าตั้งแต่แรก** จึงเลือกไม่ได้ ไม่ใช่เลือกแล้วโดนปฏิเสธ
  (`mothersInPen` กับ `mothersInBag` เป็นคนละอาเรย์ · ต้อง `moveMother` เข้ากระเป๋าก่อน)
  → **ไม่ต้องมีสิ่งกั้นเชิงกายภาพระหว่างลานคอกกับเลนรบ** กำแพงกันเรื่องนี้ไม่ได้อยู่แล้ว
- **แม่ในสนามรบ = `battleRoster`** (Phase 3C-1) ที่อยู่ที่ 3 ของแม่ · `SendMotherToBattleRequest(uid)`
  ย้ายออกจากกระเป๋า (ช่องกระเป๋าว่างทันทีตอนส่ง) · **เพดาน 10 ตัว** (`Combat.MAX_BATTLE_MOTHERS`)
  · แม่แต่ละตัวตีวินาทีละครั้งด้วย `Config.computeBattlePower` **เฉพาะตอนเปิดอัญเชิญ**
  · ⚠️ **ด่านที่กำลังตีพัง = แม่ใน roster ตายทั้งหมดพร้อมกัน** ดึงกลับไม่ได้
  · ปฏิเสธการส่งถ้า lock ไว้ / roster เต็ม / ไม่มีด่านที่มี HP ให้ตี (ด่าน 1 หรือพังครบแล้ว)
  · ค่าเก่า `Inventory.MAX_TEAM_*` (ดีไซน์ "ทีม" ที่ไม่เคยเขียนโค้ด) ยังอยู่แต่ไม่มีโค้ดใช้
- ปุ่ม **"เลือกสัตว์เลี้ยงที่ดีที่สุด"** เป็นปุ่มกดเอง ไม่ใช่ระบบที่ทำงานเอง
  กดแล้วจัดแม่เข้าคอกเรียงตามความสามารถผลิตเงิน · ย้ายกลับกระเป๋าเองได้ตลอด
- ⚠️ **ความคืบหน้าสะสมถาวร** ทหารฝ่ายรับที่เหลือ + HP กำแพงที่เหลือของแต่ละด่าน
  เซฟลง DataStore (`stageProgress` array 9 ช่อง) ออกเกม/เซิร์ฟรีสตาร์ทแล้วไม่รีเซ็ต
  ไม่งั้นด่านที่ใช้เวลานานกว่า session เดียวจะไม่มีวันผ่าน
- UI ต้องแสดง % ความคืบหน้า (ทหารเหลือกี่ % / กำแพงเหลือกี่ %) และแยกสองแถบ: แถบแม่ / แถบลูก
- **ตัวคูณ damage = ของที่ซื้อด้วยเงิน ไม่ได้ฟรีตามด่านแล้ว**
  `ตัวคูณ = 1.1 ^ damageLevel` · 72 ขั้น · **เพดาน = `wallProgress × 8`**
  ⚠️ **off-by-one**: ด่าน 1 (พังกำแพง 0 ด่าน) ต้องซื้อได้ถึงขั้น **8 ไม่ใช่ 0**
  ไม่งั้นผู้เล่นใหม่ตันตั้งแต่ด่านแรก · `validate()` assert ข้อนี้ตรง ๆ
  **ตัวคูณเป็นของบัญชีผู้เล่น ไม่ใช่ของแม่รายตัว** → แม่ตายก็ไม่เสียการลงทุน
  ราคาคิดจาก "40% ของรายได้ทั้งด่าน" ไม่ได้เดา (`docs/data-schema.md` §8.6)
- **turret เก็บเป็น "สัดส่วน" ไม่ใช่ตัวเลข damage** — `Config.Balance.Combat.TURRET_TOLL`
  ไล่ 10% (ด่าน 2) → 20% (ด่าน 9) · `turretDps(N) = TOLL[N] × getReferenceDps(N)`
  ⚠️ **ตาราง `Stages` ไม่มีฟิลด์ `turretDps` แล้ว ห้ามเอากลับมา**
  เคยเก็บเป็นตัวเลขดิบแล้วต้องคำนวณมือใหม่ **5 รอบ** ทุกครั้งที่แก้ตัวคูณคลาส /
  ตัวคูณ damage / อัตราปล่อย / อัตราผลิต / ความจุคอก และพลาดมาแล้ว 2 ครั้ง
  ตอนนี้แก้อะไรก็ตาม **turret ขยับตามเอง**
  สูตร: `กำลังพลที่เสีย = turretDps ÷ damage/วินาทีของเรา` (เวลาตัดกันออกทั้งสองฝั่ง
  เพราะ HP ของตัวเรา = damage ของตัวเรา)
  ⚠️ อิงผู้เล่นที่ **ซื้อ upgrade ครบเพดาน** → คนที่ยังไม่ซื้อเจอ turret เขี้ยวกว่า
  ตามที่เขาขาด **ซึ่งตั้งใจ** ไม่ใช่บั๊ก
  `assertTurretIsSurvivable()` บังคับ: ครบ 9 ด่าน · ด่าน 1 = 0 · ≤ 35% · ไล่ขึ้นไม่ถอยหลัง

### บอสและการแย่งไข่
- บอสรีเกิดทุก **5 นาที** ตอนเกิดมี **ไข่ 5 ฟอง** วางในรัง
- ทุกคนช่วยกันตีบอสได้ **การตีบอสเป็นแค่กิมมิค กลไกจริงคือการแย่งไข่**
- หยิบไข่ต้อง **กดค้าง 3 วินาที** ถูกตีระหว่างนั้น = เริ่มนับใหม่
- ⚠️ **server เป็นคนตัดสินเจ้าของไข่เท่านั้น** ห้าม client ตัดสินเด็ดขาด
- ไข่ที่ไม่มีใครหยิบหายตอนบอสรีเกิดรอบถัดไป · สวนฟักเต็ม 50 ฟองแล้วหยิบไม่ได้
- ผู้เล่นตีกัน/ตายได้ **เฉพาะในพื้นที่บอส** นอกพื้นที่ตีกันไม่ได้ ตายไม่ได้

### ตัวเลขที่ล็อกกันไว้ (ห้ามแก้ข้างเดียว)
- **damage อาวุธ ×10 ต่อขั้น = HP บอส ×10 ต่อด่าน** → อาวุธขั้น N ตีบอสด่าน N ตาย 10 ครั้งพอดีทุกด่าน
- **เงิน ×10 ต่อด่านที่พังกำแพงได้** → ถ้าตัดออก ผู้เล่นจะซื้อของขั้นกลางขึ้นไปไม่ได้เลย
  (ราคาทุกอย่างโต ×10 ต่อขั้น แต่รายได้จากน้ำหนักโตแค่ ×1,000 ตลอดเกม เพราะ sqrt บีบไว้
  **การดันค่าเริ่มต้นขึ้นแก้ไม่ได้** เพราะปัญหาคืออัตราการโตไม่เท่ากัน ไม่ใช่ตัวเลขเล็กไป)
- **upgrade อัตราผลิต ×3 ต่อขั้น (ราคา ×10)** → ถ้าผลเป็น ×10 ด้วย กำแพงจะหมดความหมายหลังด่าน 5
  **เพดาน 5 ขั้น** (ตัดจาก 10) · คอก **เพดาน 10 ขั้น** (ตัดจาก 15)
  ขั้นบน ๆ เป็นของตายที่ไม่มีใครไปถึง (คอก Lv15 ราคา 1e17 แต่รายได้ทั้งด่าน 9 มี 1.28e14)
  ⚠️ ตัดลึกกว่านี้ไม่ได้ — `validate()` บังคับว่าด่าน 3–9 ต้องยัง **ผลิตได้ ≥ ปล่อยได้**
  ไม่งั้นคอขวดย้อนกลับไปเป็นการผลิตแล้วเวลาตีจะเปลี่ยนโดยไม่ตั้งใจ
- **`Config.Balance.DamageUpgrade` — 8 ขั้น/ด่าน ×1.1 รวม 72 ขั้น** (สั่งมา 9 ขั้น/ด่าน แต่ขัดกับ "72 ขั้น" ที่สั่งมาเอง)
  9 ขั้น/ด่าน = 81 ขั้น และทำให้ด่าน 4 จบใน 0.502 ชม. เฉียดพื้น 0.5 ชม. แบบไม่มีระยะเผื่อ
  (เพดานจริงคือ ×1.10014 ต่อขั้น) · **8 ขั้นตรงกับ 72 พอดีและมีระยะเผื่อทั้งสองด้าน**
  ด่าน 2 = 0.9 ชม. ไต่ถึงด่าน 9 = 54 ชม. · ซิกแซกตามขั้นคลาส หลุมลึกสุด 2.6 เท่า
- **ราคา upgrade ต้องกินรายได้ของด่านนั้น 30–70%** (ตั้งไว้ ~40%)
  ถูกไป = เงินล้นเหมือนเดิม · แพงไป = ผู้เล่นตัน · `validate()` บังคับ
- **ตารางอัตราปล่อย `{1,1,1,2,3,4,6,8,10}`** ห้ามเร่งด่าน 3 เป็น 2
  เพราะคอขวดพลิกจาก "ผลิต" เป็น "ปล่อย" ตรงนั้นพอดี เร่งแล้วเส้นเวลาจะแอ่นลง
- ⚠️ **`assertProgressionIsSane()` ใน Config** บังคับว่าทุกด่านต้องใช้เวลา 0.5–72 ชม.
  และด่านท้ายต้องไม่เร็วกว่าด่านแรก — ตั้งค่าที่ทำให้เกมพัง เซิร์ฟจะไม่บูต
- ⚠️ **`assertTurretIsSurvivable()` ใน Config** บังคับว่า `TURRET_TOLL` ไม่เกิน 35%
  ต่อด่านและไล่ขึ้นไม่ถอยหลัง — เรื่องนี้พลาดมาแล้วสองรอบและมองไม่เห็นด้วยตาเปล่า
  จึงย้ายมาเก็บเป็นสัดส่วนแทนตัวเลขดิบ เพื่อให้พลาดแบบเดิมไม่ได้อีก
- ⚠️ **ยามเวลาฟาร์ม: เวลาหาไข่ต้องไม่เกิน 5 เท่าของเวลาตีกำแพง**
  เคยพลาด: ยามผ่านทุกด่าน ทั้งที่ของที่ยามสมมติว่าผู้เล่นมี (แม่ tier 6 = 1 ใน 111,111)
  ต้องฟาร์มหลักหมื่นชั่วโมง **ยามต้องวัดผู้เล่นที่มีอยู่จริง ไม่ใช่ผู้เล่นในฝัน**
  `REFERENCE_WEIGHT` จึงคงที่ที่ tier 1 (500 kg) ทุกด่าน แล้วให้คลาสไต่แทน

### สูตร (เลือกแล้ว: รากที่สอง ทั้ง damage และเงิน)
- `damage = HP = 10 × (น้ำหนัก/100)^0.5 × ตัวคูณคลาส × 1.1^damageLevel` — ลูกแรง 10% ของแม่เสมอ
- `coins/นาที = 1 × (น้ำหนักแม่/100)^0.5 × 10^(wallProgress-1)` — คลาสไม่มีผลต่อเงิน

## Stack & เครื่องมือ
- **Roblox Studio** — เปิดไฟล์ place ที่ build จาก Rojo
- **Rojo** — sync โค้ดในโฟลเดอร์นี้เข้า Studio (Claude Code แก้ที่ไฟล์ .lua ในโฟลเดอร์นี้เท่านั้น)
- **Luau** — ภาษาสคริปต์
- **DataStore** — บันทึกข้อมูลผู้เล่น

คำสั่งที่ใช้บ่อย:
```
rojo serve          # dev server ให้ Studio plugin เชื่อม
rojo build -o game.rbxl   # build ไฟล์ place
```

## โครงสร้างโฟลเดอร์
```
src/
  server/   → ServerScriptService (logic ฟักไข่, คอก/กระเป๋า, combat, DataStore)
    Main.server.lua      → ServerScriptService.Main (entry point, ต่อสายอย่างเดียว)
    MapBuilder.lua       → ⚠️ โครงหลัก: สร้างแมพทั้งใบด้วยโค้ด (ลานคอก/เลนรบ/รังบอส/ร้านค้า)
    PenService.lua       → จอง/คืนคอก + วาดแม่ที่เดินได้และไข่ลงในคอก
    EggService.lua       → สร้างไข่ (พร้อมน้ำหนัก) จับเวลา ฟักเป็นตัวแม่ คอก/กระเป๋า
    DataService.lua      → ⚠️ โครงหลัก: **จุดเดียวที่แตะ DataStore ได้** · session lock · retry
                            · autosave · BindToClose · รับของปลอมเข้าไปทดสอบได้
    ProductionService.lua → settle ผลิตลูก+เงินจากแม่ในคอก (คำนวณจาก timestamp ไม่ใช่ loop นับสด)
    CombatService.lua    → Phase 3A: ปล่อยทหารจากคลัง (releaseOrder) + ตี defendersRemaining/
                            wallHpRemaining ต่อด่าน + จ่ายเงินตามสัดส่วน HP ทหารฝ่ายรับที่ลด +
                            auto-pause · ไม่มี offline catch-up (รับ elapsedSeconds ตรง ๆ)
                            · Phase 3C-1: ส่งแม่ลง `battleRoster` + แม่ตาย/ล้าง roster ตอนด่านพัง
  client/   → StarterPlayerScripts (UI ทั้งหมด + ของที่เห็นเฉพาะตัวเอง)
    Main.client.lua      → StarterPlayerScripts.Main (entry point)
    WallRenderer.lua     → ⚠️ วาดกำแพงตาม `stageProgress` จริงจาก sync (ไม่ใช่ wallProgress)
                            **ต้องอยู่ฝั่ง client เท่านั้น**
    TroopRenderer.lua    → Phase 3B-1: โมเดลทหารฝ่ายเรา/ฝ่ายรับ (คนบล็อก ๆ จาก Part ไม่ใช่
                            Humanoid) เป็นแค่ simulation ภาพประกอบ ไม่ผูกกับ damage จริง 1:1
  shared/   → ReplicatedStorage.Shared (config, constants, type ที่ใช้ร่วมกัน)
    init.lua             → ตัว Shared เองเป็น ModuleScript (เป็นแค่ฝา)
    Config.lua           → ⚠️ โครงหลัก: ตัวละคร/คลาส tier น้ำหนัก ตารางคลาสของไข่
                            อัตราผลิต สถานะ stack key สูตร damage/เงิน
                            คอก/กระเป๋า/สวนฟัก ด่าน กำแพง บอส อาวุธ ชื่อ RemoteEvent
    Remotes.lua          → สร้าง/รอหา RemoteEvent
    PlayerData.lua       → ⚠️ โครงหลัก: โครง PlayerData + template ผู้เล่นใหม่ + migration
                            + กฎ encode (§9.3) + วัดขนาด · **ไม่แตะ Roblox API เลย** จึงเทสต์ได้นอก Studio
docs/
  data-schema.md       → ⚠️ โครงหลัก: schema ของ PlayerData + ที่มาของตัวเลขสมดุล
                            + แผน migration + จุดเสี่ยง exploit + ProcessReceipt
  map-layout.md        → ⚠️ โครงหลัก: ผังแมพ พิกัดทุกโซน และกฎ client/server
  hotbar-design.md     → ดีไซน์ Hotbar 10 ช่อง (Phase 5.5 · ยังไม่ได้เขียนโค้ด)
  phase-1.5-rework.md  → บันทึกการรื้อโค้ด Phase 1 ให้ตรงดีไซน์ใหม่ (ทำครบแล้ว) + เช็คลิสต์ที่ต้องทดสอบใน Studio
  debug-commands.md    → คำสั่ง debug ทั้งหมดที่เรียกผ่าน command bar ใน Studio (ไม่ใช่ฟีเจอร์ในเกม)
  phase-3c-report.md   → รายงานสรุป Phase 3C-1 + 3C-2 (ส่งแม่ไปรบ + ตายถาวร) + ผลทดสอบ Studio
default.project.json   → mapping ของ Rojo
```

⚠️ **ห้ามวาง `init.server.lua` / `init.client.lua` ที่รากของ `src/server` หรือ `src/client`**
ไฟล์ init จะเปลี่ยน "ตัวโฟลเดอร์" ให้เป็น Script ทำให้ `ServerScriptService` /
`StarterPlayerScripts` ไม่ใช่ service อีกต่อไป → เกมพัง
entry script ใช้ชื่อ `Main.server.lua` / `Main.client.lua` เท่านั้น

## แผนพัฒนาเป็นเฟส
ทำทีละเฟส **อย่าข้ามไปทำเฟสถัดไปเองถ้ายังไม่สั่ง**

- **Phase 0** — โครงโปรเจกต์ + Rojo + README + .gitignore ✅
- **Phase 1** — ฟักไข่ + ฟาร์ม (**กลไกเก่า**) ✅
  ⚠️ โค้ดทำตามกลไกเก่า "ฟัก → ได้ทหาร → เข้าคลัง" **ต้องรื้อใน Phase 1.5**
- **Phase 1.5** — รื้อ Phase 1 ให้ตรงดีไซน์ใหม่ (**ยังเป็น in-memory ยังไม่แตะ DataStore**) ✅
  **ไข่มีน้ำหนักตั้งแต่ตอนสร้าง** → ฟักแล้วสุ่มแค่ตัวละคร → ได้ตัวแม่
  · `heldEggs` เก็บไข่รายฟอง (`PlaceEggInHatcheryRequest` ส่ง index ไม่ใช่ชนิดไข่)
  · `PlotService` → `PenService` (คอก + กระเป๋า) · ตัดปุ่มวางไข่ฝั่ง client
  · `os.clock()` → `os.time()`
  📄 บันทึกว่ารื้ออะไรไปบ้าง + เช็คลิสต์ที่เหลือ อยู่ใน `docs/phase-1.5-rework.md`
  ⚠️ **ยังไม่ได้เปิด Studio ทดสอบ** — โค้ดผ่านแค่ `rojo build` + `luau-lsp analyze` + `Config.validate()`
- **Phase 2A** — DataStore เซฟ/โหลด + โครง `heldEggs` ใหม่ ✅
  `DataService` (UpdateAsync · session lock · retry 3 ครั้ง · autosave 60 วิ แบบกระจาย ·
  BindToClose) · `PlayerData` (template · migration · กฎ encode · วัดขนาด)
  · กระเป๋าไข่ 50 → **10,000** · `PlaceEggInHatcheryRequest` ส่ง **id ประจำฟอง** ไม่ใช่ตำแหน่ง
  ⚠️ **ยังไม่ได้ทดสอบใน Studio** — ต้อง publish + เปิด API ก่อน (ดู README)
- **Phase 2B** — ผลิตลูก + ผลิตเงิน + อัปเกรดคอก + ขายแม่ + ข้อ D (คอก+กระเป๋าเต็มพร้อมกัน) ✅
  เขียนโค้ดเสร็จแล้วทั้งหมด (2B-1: ผลิตลูก/เงิน · 2B-2: อัปคอก/ขายแม่/ข้อ D) ยังไม่ได้ทดสอบใน Studio
- **Phase 3** — แมพ 9 ด่าน + กำแพง + ทหารฝ่ายรับ (HP รวม) + **ปล่อยทหารต่อเนื่อง (Age of War)**
  + ปุ่มอัญเชิญ + auto-pause + `stageProgress` สะสมถาวร + จัดทีม + ตายถาวร + UI สองแถบ (แม่/ลูก)
  - **3A** (เครื่องยนต์คำนวณรบฝั่ง server — ไม่มีภาพ) ✅ `CombatService.lua`
  - **3B-1** (ภาพ+UI พื้นฐาน — ทำงานได้จริง ยังไม่ polish) ✅ `WallRenderer`/`TroopRenderer`/UI
  - **3B-2** (กำแพงแตกตาม % HP · เลขความเสียหายลอย · เอฟเฟกต์ตอนตี) ยังไม่ทำ
  - **3C-1** (ส่งแม่ไปรบ + ตายถาวร ฝั่ง server — `battleRoster` · schema v2) ✅
  - **3C-2** (UI ปุ่ม "ส่งไปรบ" ในแท็บกระเป๋า + กล่องยืนยัน + บรรทัด roster ใน CombatHud) ✅
    ⚠️ ปุ่ม "ส่งไปรบ" **แค่เปิดกล่องยืนยัน** · remote ยิงจากปุ่ม "ยืนยันส่งรบ" ที่เดียว · "ยกเลิก" ไม่ส่งอะไร
  📄 รายงานสรุป 3C-1 + 3C-2 (ตัดสินอะไร · ผลทดสอบ Studio · ที่ยังขาด) อยู่ใน `docs/phase-3c-report.md`
- **Phase 4A** — **รางวัลผ่านด่าน** (ไข่ฟรีครั้งเดียวต่อด่านตอนกำแพงพังครั้งแรก) ✅ ทดสอบใน Studio แล้ว ผ่านครบ
  ⚠️ ชื่อ "4A" ตั้งตามที่สั่ง — **คนละเรื่องกับ Phase 4 (รบด้วยตัวเอง) ข้างล่าง** ซึ่งยังไม่ได้เริ่ม
  ด่าน 1 = 0 (ไม่มีกำแพง) · 2–3 = 1 · 4–6 = 2 · 7–9 = 3 · ไข่ = `egg_stageN` ผ่าน `grantEgg`
  · แจ้งด้วย `StageClearedNotify` (server → client ครั้งเดียว ไม่อยู่ใน sync) · รายละเอียด `docs/data-schema.md` §7.12
  📄 วิสัยทัศน์อนาคต **"HP รายตัว + turret"** (ลูก/แม่มี HP รายตัวจริง · turret สุ่มยิงมีจังหวะ
  ของตัวเอง · ทหารฝ่ายรับตีกลับได้ · หลอด HP ลอยทุกตัว) เป็น**เฟสแยกทีหลัง Phase 3C** —
  ยังไม่ได้ตั้งเลขเฟส **ไม่บล็อก 3C** ดู `docs/combat-hp-vision.md`
- **Phase 4** — ระบบรบด้วยตัวเอง: อาวุธ · ตี · ตาย · เกิดใหม่ (จำกัดในพื้นที่บอส)
- **Phase 5** — บอส + รีเกิด 5 นาที + ไข่ 5 ฟอง + กติกาแย่งไข่ (กดค้าง 3 วิ) + ร้านอาวุธ
- **Phase 5.5** — **Hotbar 10 ช่อง** (ช่องถือของ · hotkey 1-9,0 · มือถือ 4-5 ช่อง)
  📄 ดีไซน์อยู่ใน `docs/hotbar-design.md` · **ยังไม่ได้เขียนโค้ด ยังไม่มี schema**
  ⚠️ **แต่ละช่องเป็น shortcut ชี้ไปที่ของในกระเป๋าจริง ไม่ใช่ inventory ใหม่**
  สแต็คตามธรรมชาติของของที่ชี้ไป (ลูกสแต็ค · ไข่สแต็คตาม eggId+น้ำหนัก · แม่ไม่สแต็ค)
  **คนละอันกับกระเป๋าแม่ 100 และกระเป๋าไข่ 10,000**
  รอถึงเฟสนี้เพราะก่อนหน้านั้นยังไม่มีของให้ใส่นอกจากไข่กับแม่
  ⚠️ มี 4 ข้อที่ยังไม่ตัดสิน (แสดงของที่ถือบนตัวยังไง · ของหมดแล้วล้างช่องหรือ dim ·
  สั่งงานจากช่องได้เลยไหม · ปุ่มอัปเกรดไม่กินช่อง) — ดู `docs/hotbar-design.md` §4
  📄 ส่วนขยาย **"หยิบ-วางจริงในโลก 3D"** (ไข่/แม่/ทหาร) เป็น**เฟสแยกทีหลัง** ไม่พ่วงมากับ 5.5 —
  ยังไม่ตั้งเลขเฟส ดู `docs/hotbar-design.md` §7 (มีของใหม่: หยิบ-วางทหารที่จุดปล่อยแทนแผง
  จัดคิวปล่อยทหาร 2D ปัจจุบัน ซึ่งเป็นทางลัดชั่วคราวตามที่ตกลงไว้ตอน 3B-1 ไม่ใช่ของถาวร)
- **Phase 6** — Robux / Developer Product + ไข่ตำนาน (ProcessReceipt ที่ปลอดภัย)
- **Phase 7** — สมดุลตัวเลข + ระบบติดสถานะให้แม่ (gold/silver) + polish
- **Phase 8** — ทดสอบ 6 ผู้เล่นพร้อมกัน + publish

## กฎการทำงาน

### ใช้ skill `confirm-core-changes` เสมอ
ก่อนแก้/ลบ/เปลี่ยนชื่อ/รื้อ "โครงหลัก" ให้ทวนก่อนแล้วรอยืนยัน โครงหลักของโปรเจกต์นี้คือ:
- `default.project.json` (mapping ของ Rojo — พังแล้ว sync ไม่ได้ทั้งโปรเจกต์)
- **schema ของ DataStore** — ชื่อ key, โครงสร้าง PlayerData, `schemaVersion`
  (ดู `docs/data-schema.md` — แก้แล้วต้องเขียน migration ด้วยเสมอ)
  · ตอนนี้ **v3** (v1→v2 = `battleRoster` · v2→v3 = `stageClearBonusGranted` · ประวัติใน `docs/data-schema.md` §10.4)
- **schema ของ mothers / children** — ฟิลด์ในตัวแม่, การแยก `mothersInPen` /
  `mothersInBag` / `battleRoster` เป็นสามอาเรย์ (แม่ 1 ตัวอยู่ได้ที่เดียว), การเก็บลูกเป็นกองไม่ใช่รายตัว,
  `uid` ที่เป็น **global string** และห้าม reuse, `nextUid` ที่ห้ามลด
- **รูปแบบ stack key** — ตัวคั่น, กฎเรียงสถานะ, การใช้ charId + น้ำหนักแม่เป็น identity
  (เปลี่ยนเมื่อไหร่ = กองลูกของผู้เล่นทุกคนอ่านไม่ออก เจ็บที่สุดในโปรเจกต์)
- **`wallProgress`** — เป็นทั้งสิทธิ์เข้าพื้นที่บอสและตัวคูณเงิน และต้องอยู่ต่อผู้เล่นเสมอ
- **`stageProgress` — ความคืบหน้าต่อด่าน** (ทหารฝ่ายรับที่เหลือ + HP กำแพงที่เหลือ ทั้ง 9 ด่าน)
  เป็น array ยาวคงที่ ช่องว่างใช้ `false` — แก้โครงเมื่อไหร่ = ผู้เล่นทุกคนที่ตีค้างไว้เสียงานทิ้ง
- **`stageClearBonusGranted` — ธงรางวัลผ่านด่าน 9 ช่อง** (Phase 4A · schema v3) อาเรย์ boolean ยาวคงที่
  index ตรงกับ `stageProgress` · ติดเฉพาะใน `CombatService.claimStageClearBonus` ตอนด่าน**เพิ่งพัง**ใน tick
  ⚠️ **ห้ามย้ายการให้รางวัลไป `recomputeWallProgress`** (ไล่นับด่านที่พังอยู่ใหม่ทุกครั้ง = ผู้เล่นเก่าได้ไข่ย้อนหลัง)
  · false ≠ "ยังไม่พัง" (ผู้เล่นก่อน v3 ได้ false ทั้งหมดโดยตั้งใจ) · ตารางจำนวนไข่ `Balance.Combat.STAGE_CLEAR_BONUS_EGGS`
- **cap คลังทหาร** (`STACK_CAP` = 500 ตายตัว) — ผูกกับ cap ออฟไลน์ 8 ชม.
  ลดค่าลงเมื่อไหร่ = ลูกที่ผู้เล่นสะสมเกินเพดานหายทันที
- **โครงของไข่** — `heldEggs` / `hatching` เก็บ **รายฟองพร้อมน้ำหนัก** ไม่ใช่ตัวนับ
  และไข่รายด่าน `egg_stage1..9` ที่ทุกด่านต้องมีครบ (ขาดด่านไหน = ด่านนั้นตันถาวร)
- **`Config.Balance.BalanceCheck`** — ผู้เล่นอ้างอิงที่ยามทั้งสามตัวใช้วัด
  ตั้งผิด = ยามผ่านทั้งที่เกมเล่นไม่ไหว (เคยเกิดมาแล้ว)
- **สูตรเงินจากการฆ่า** (`KILL_DEFENDER_*` / `KILL_BOSS_*`) — ต้องโตไม่ช้ากว่าราคาของ
- **สูตรอัตราผลิต** (`WEIGHT_EXPONENT` / `ONLINE_PER_MINUTE` / `OFFLINE_RATE_RATIO`)
  — เป็นตัวตั้งของทั้ง damage/วินาที เวลาเติมคลัง และเส้นเวลาของทุกด่าน
- **ตัวคูณคลาส** (`CharacterClasses[x].multiplier`) — เป็นแกนไต่ของทั้งเกม
  แก้เมื่อไหร่ต้องคำนวณราคา `DamageUpgrade` และ `turretDps` ใหม่ทั้งชุด
  และห้ามดัน SS ให้ห่างจาก S เกิน 2.5 เท่า (pay-to-win)
- **`Config.Balance.DamageUpgrade`** — ตัวคูณต่อขั้น, ขั้นต่อด่าน, `MAX_LEVEL`,
  **ตาราง `STAGE_COST_BASE`** และสูตรเพดาน `wallProgress × 8`
  ราคาคิดมาจากรายได้ต่อด่าน แก้ข้างเดียวแล้วเงินจะล้นหรือผู้เล่นจะตัน
  · `damageLevel` ใน PlayerData ฝังอยู่ในข้อมูลที่เซฟไปแล้ว
- **`Config.Balance.Combat`** — อัตราปล่อย, `TURRET_TOLL`, เกณฑ์ auto-pause
  และธง `ALLOW_AUTO_RELEASE_MOTHERS` / `MOTHERS_SELECTABLE_FROM_PEN` ที่ `validate()` ตรึงไว้ว่าต้องเป็น false
- **การแบ่งข้อมูล per-player vs per-server** — อะไรเซฟลง DataStore อะไรอยู่ใน memory
  (ตารางใน `docs/data-schema.md` §2 — สลับผิดข้างแล้วผู้เล่นเสียของหรือได้ของฟรี)
- **`charId` / `statusId` / `eggId` / `unitId`** — ฝังอยู่ในข้อมูลที่เซฟไปแล้ว
  ห้ามเปลี่ยนชื่อ ห้าม reuse (เลิกใช้ให้ตั้ง `enabled = false` แทนการลบ)
- **config กลางใน `src/shared`** — ตัวละคร/คลาส, tier น้ำหนัก, ตารางคลาสของไข่,
  `CHILD_RATIO`, อัตราผลิต, สูตร damage/เงิน, ราคาทุกอย่าง, เพดานคลัง
- **`Config.Balance` — ลูกบิดสมดุลทั้งหมดอยู่ใต้ชื่อเดียว** 18 กลุ่ม:
  `Weight` · `StageWeightTiers` · `Production` · `Damage` · `NewPlayer` · `Economy` ·
  `Pen` · `Bag` · `Hatchery` · `Stages` · `Stage` · `Boss` · `DamageUpgrade` ·
  `SpeedUpgrade` · `Combat` · `BalanceCheck` · `Weapon` · `VisualScale`
  **ชื่อคีย์ข้างในคงเดิมทั้งหมด** ย้ายแค่ที่อยู่
  ⚠️ `validate()` บังคับสองทาง: ต้องมีครบทุกกลุ่มใน `Config.Balance`
  **และต้องไม่มีชื่อเดียวกันที่ `Config` ชั้นบนสุด** — เติมกลับเข้าไปเมื่อไหร่ เซิร์ฟไม่บูต
  (เพิ่มกลุ่มสมดุลใหม่ต้องเติมชื่อใน `BALANCE_GROUPS` ด้วย ไม่งั้นยามมองไม่เห็น)
  ของที่ **ห้าม** ย้ายเข้ามา: `MapDimensions` · `RemoteNames` · `DataStore` · `Stack` ·
  `Uid` · `World` · `Inventory` · ตารางที่เป็น id (`Characters` · `EggTypes` · `Statuses`)
- **เวลาฟักไข่** — ไข่ source="boss" ฟักตาม **น้ำหนัก+คลาส** ผ่าน `Config.getHatchSeconds()`
  (`Config.Balance.Hatchery.HatchTimeByTier` × `ClassHatchMultiplier`) ต้องรู้คลาสก่อนตั้งเวลา
  จึงต้อง `Config.rollCharacter()` **ตอนวางไข่ลงสวนฟัก** (ไม่ใช่ตอนฟักเสร็จ) เก็บผลไว้ใน
  `HatchSlot.charId` แบบไม่บอก client จนกว่าจะฟักเสร็จจริง · ไข่ source="robux" ฟักคงที่
  `LEGENDARY_SECONDS` เสมอ ไม่ผ่านสูตรนี้ · `hatchTime` ในตาราง `EggTypes` (คำนวณจาก
  `Config.Balance.Hatchery.SECONDS_PER_STAGE` เหมือนเดิม ห้ามใส่ตัวเลขดิบ · `validate()` บังคับสูตรนี้
  เหมือนเดิม) **เหลือเป็น metadata ของไข่รายด่าน ไม่ใช่เวลาฟักจริงอีกต่อไป** — ยังไม่ลบเพราะ
  core-locked (ไข่ที่ `enabled = false` ยกเว้นจากสูตรนี้ — แช่แข็งไปแล้ว)
- **`heldEggs` — `{ nextEggId, items }`** ไม่ใช่อาเรย์ยาวคงที่แล้ว · ไข่แต่ละฟองมี `id` ประจำตัว
  **ห้ามลด `nextEggId` ห้าม reuse id** (เหตุผลเดียวกับ `nextUid`) ·
  ห้ามแตะ `items` ตรง ๆ ใช้ `PlayerData.addHeldEgg/findHeldEgg/removeHeldEgg` เท่านั้น
  · `PlaceEggInHatcheryRequest` ส่ง **id** ไม่ใช่ตำแหน่ง — ส่งตำแหน่งแล้วจะวางผิดฟองเวลาไข่ฟักคั่นจังหวะ
- **`sessionLock` ใน PlayerData** — กันผู้เล่นคนเดียวเปิดสองเซิร์ฟเวอร์พร้อมกัน
  ถอดออกเมื่อไหร่ = สองเซิร์ฟเวอร์ถือข้อมูลคนละชุด ใครเซฟทีหลังทับของอีกคนทั้งชุด
- **`Config.DataStore`** — `NAME` / `KEY_PREFIX` (เปลี่ยน = ผู้เล่นเก่าหาข้อมูลตัวเองไม่เจอ) ·
  `SESSION_LOCK_SECONDS` · `AUTOSAVE_INTERVAL` / `AUTOSAVE_STAGGER` · `RETRY_*` ·
  `BIND_TO_CLOSE_SECONDS` (ต้อง < 30 · Roblox ตัดที่ 30) · `MAX_PLAYER_DATA_BYTES` ·
  `SAVE_WAIT_LIMIT` / `SAVE_WAIT_STEP`
  ⚠️ `validate()` ผูกค่าพวกนี้เข้าหากันแล้ว — lock ต้องยาวกว่ารอบเซฟที่ช้าสุด ฯลฯ
- ⚠️⚠️ **ตอนปิดเซิร์ฟต้องไล่จากแคช ไม่ใช่ `Players:GetPlayers()`** (`DataService.pendingOnClose`)
  ตอนคนสุดท้ายกด Leave, `PlayerRemoving` ยิงก่อน `BindToClose` เสมอ → รายชื่อผู้เล่นว่างไปแล้ว
  แต่เซฟของเขายังค้างกลางทาง · ไล่จากรายชื่อ = เซิร์ฟตายทับเซฟที่ยังวิ่ง
- ⚠️⚠️ **เซฟรอบสุดท้าย (ออกเกม/ปิดเซิร์ฟ) ต้องรอรอบที่ค้างอยู่ให้จบ ห้ามข้าม**
  ตัวกัน "เซฟซ้อน" ใช้ได้กับ autosave เท่านั้น · ข้ามเมื่อไหร่ = ของที่ได้มาหลัง autosave รอบนั้นหายหมด
  และ `sessionLock` ไม่ถูกปลดด้วย (เข้าเกมใหม่ไม่ได้ 5 นาที) — เคยเกิดจริง ดู `docs/data-schema.md` §9.4
- **`productId` ของ Developer Product และการจัดการ `ProcessReceipt`** — พลาดแล้ว
  ผู้เล่นจ่ายเงินจริงแล้วไม่ได้ของ หรือได้ของซ้ำจากการจ่ายครั้งเดียว
- **`Config.Balance.SpeedUpgrade`** — ราคา/จำนวนขั้น/`CURVE_EXPONENT`/`MAX_MULTIPLIER`/`SPEED_CEILING`
  · `speedLevel` ฝังอยู่ในข้อมูลที่เซฟไปแล้ว · **ห้ามขยายเป็น 10 ขั้น** (เหตุผลข้างบน)
  · แก้ความเร็วเมื่อไหร่ **ต้องคำนวณความหนากำแพงใหม่ทั้งชุด** (`getMinWallThickness`)
- **ความหนากำแพงทุกชนิด** (`StageWall.Thickness` · `Lane.WallThickness` · `Boundary.Thickness`)
  — ผูกกับความเร็วสูงสุด บางกว่าเกณฑ์ = ผู้เล่นวิ่งทะลุ (`Pen.FenceThickness` ยกเว้น — ไม่ชน)
- **`Config.MapDimensions` — รูปทรงของแมพทั้งใบ** ขนาดคอก · ขนาดเลน · ห้องบอส ·
  แผงร้าน · ขอบแมพ · ความเร็ววิ่ง/ความสูงกระโดดผู้เล่น · รั้ว/ประตู/ป้าย · แม่เดินไปมา
  แก้แล้วของทุกอย่างในโลกขยับตาม และ `validate()` บังคับว่าโซนห้ามทับกัน
  · **ไม่มี `Config.Map` แล้ว** (ยุบ alias ทิ้ง) มีชื่อเดียว
- **กฎ "อะไรวาด client อะไรอยู่ server"** — กำแพง/ทหารฝ่ายรับ/กองทัพตัวเอง **ต้องอยู่ client**
  ส่วนคอก/แม่/บอส/ไข่ในรัง **ต้องอยู่ server** · ย้ายข้างเมื่อไหร่ = การชนแยกคนพัง
  หรือผู้เล่นเห็นของที่ไม่ควรเห็น (ตารางเต็มใน `docs/map-layout.md` §2)
- **จำนวนคอก = `PLAYERS_PER_SERVER`** — `validate()` บังคับสองชั้น
  (`PEN_ROWS × PEN_PER_ROW == MAX_PENS` และ `MAX_PENS == PLAYERS_PER_SERVER`)
- **RemoteEvent / RemoteFunction** — ชื่อและ signature ที่ client-server ตกลงกัน
  (รวม `StageClearedNotify(stage, eggCount)` server → client ของ Phase 4A)
- โครงโฟลเดอร์ `src/server|client|shared` และการแตก/รวมไฟล์
- อะไรก็ตามที่ทำให้ข้อมูลผู้เล่นเดิมอ่านไม่ออก

ไม่ต้องทวนเมื่อ: แก้สี/ข้อความ/UI เล็กน้อย, แก้บั๊ก scope แคบ, เพิ่มฟีเจอร์แยกอิสระ, หรือผู้ใช้บอกว่า "ทำเลย"

### กฎอื่น
- **อย่าแก้ไฟล์ `.rbxl` / `.rbxlx` โดยตรง** — เป็น binary ที่ build ออกมา แก้ที่ source เท่านั้น
- **server-authoritative** — การฟักไข่, สุ่มผล, คำนวณ combat, ให้ currency ต้องทำฝั่ง server ทั้งหมด ห้าม client ตัดสิน (กัน exploit)
- validate ทุก input ที่มาจาก client เสมอ
- ไม่ต้องเขียนโค้ดล่วงหน้าให้เฟสถัดไป ทำเฉพาะที่สั่ง
- commit เป็นก้อนย่อยตามงานที่ทำเสร็จ ข้อความ commit ภาษาอังกฤษสั้น ๆ

## สถานะปัจจุบัน
**วางโครงแมพ (blockout) เสร็จแล้ว** — `MapBuilder` generate ทั้งแมพจากโค้ด
ลานคอก 6 แปลง · เลนรบ 9 ด่าน · รังบอส 9 รัง · ร้านค้า · กำแพงวาดฝั่ง client
📄 ผังเต็มใน `docs/map-layout.md` · **ยังไม่มีระบบ** (ผลิตลูก/รบ/บอส เป็นเฟสถัดไป)

**Phase 1.5 เขียนโค้ดเสร็จแล้ว** — โค้ดใน `src/` ตรงดีไซน์ใหม่ทั้งหมด
(ฟักไข่ได้ **ตัวแม่** ไม่ใช่ทหาร · ไข่มีน้ำหนักตั้งแต่ตอนสร้าง · คอก + กระเป๋า + สวนฟัก 50 ช่อง)
**ยังไม่ได้เปิด Studio ทดสอบ** ผ่านแค่ `rojo build` + `luau-lsp analyze` สะอาด + `Config.validate()`
**Phase 2A เขียนโค้ดเสร็จแล้ว** — DataStore เซฟ/โหลดได้ · session lock · autosave ·
กระเป๋าไข่ 10,000 ฟองด้วยโครง `{ nextEggId, items }`
**ยังไม่ได้ทดสอบใน Studio** (ต้อง publish + เปิด Enable Studio Access to API Services ก่อน)
**Phase 2B เขียนโค้ดเสร็จแล้วทั้ง 2B-1 และ 2B-2** — ผลิตลูก/เงินจากแม่ในคอก · อัปเกรดคอก ·
ขายแม่ · ข้อ D (คอก+กระเป๋าเต็มพร้อมกัน) ครบตามที่ออกแบบไว้ · **ยังไม่ได้ทดสอบใน Studio**
**Phase 3A + 3B-1 เขียนโค้ดเสร็จแล้ว** — เครื่องยนต์คำนวณรบ (`CombatService`: ปล่อยทหารจาก
คลังตาม `releaseOrder` · ตี defendersRemaining ก่อนแล้วส่วนเกินไหลไปกำแพง · เงินตามสัดส่วน HP
ทหารฝ่ายรับ · auto-pause) ต่อครบกับภาพ+UI แล้ว (`WallRenderer` อ่าน `stageProgress` จริง ·
`TroopRenderer` โมเดลคนบล็อก ๆ · แท็บ "ลูก" โชว์ % ความคืบหน้า+ปุ่มอัญเชิญ · แผงจัดคิวปล่อยทหาร
เปิดใกล้จุดปล่อย) **ยังไม่ได้ทดสอบใน Studio** · Phase 3B-2/3C/4–7 ออกแบบเสร็จแล้ว + `Config.lua`
พร้อมแล้ว แต่ยังไม่ได้เขียนโค้ด
**Phase 3C-1 เขียนโค้ดเสร็จแล้ว (server)** — `battleRoster` + migration v1→v2 ·
`SendMotherToBattleRequest` · แม่ตีรวมกับลูก · ด่านพัง = แม่ใน roster ตายทั้งหมด
**Phase 3C-2 (UI) เขียนเสร็จแล้ว** — ปุ่ม "ส่งไปรบ" + กล่องยืนยัน + "แม่ในสนามรบ: X/10" ใน CombatHud
**ทั้ง 3C-1 และ 3C-2 ทดสอบใน Studio แล้ว ผ่านครบ** (📄 `docs/phase-3c-report.md`)
**Phase 4A (รางวัลผ่านด่าน) เสร็จและทดสอบใน Studio แล้ว** — schema v3 · ไข่ฟรีครั้งเดียวต่อด่าน + popup ·
ผ่านครบ: ผู้เล่นเก่าธง false ทั้งหมดไม่ได้ไข่ย้อนหลัง · ด่าน 2 ได้ 1 ฟอง · พังซ้ำไม่ได้ · popup ไม่โผล่ซ้ำ · ด่าน 4/7 ได้ 2/3 ฟอง

ค่าทั้งหมดใน Config ผ่านการทดสอบพฤติกรรมจริงแล้ว 457 เคส (`luau tests/run.luau`)
(สุ่มน้ำหนัก 5 ล้านครั้ง · สุ่มตัวละคร 300,000 ครั้งต่อไข่ · ไข่รายด่าน 60,000 ครั้งต่อด่าน ·
stack key · uid · บัฟสถานะ · แหล่งที่มาไข่ · Developer Product · ตารางด่าน · อัตราปล่อย ·
อัตราผลิตตามน้ำหนัก · cap คลัง · ด่าน 1 ไม่มีกำแพง · turret · ตัวคูณคลาส ·
เงินจากการฆ่า · ยามเวลาฟาร์ม · upgrade damage · ราคาที่ดูดส่วนเกิน · แบ่งเงินบอส · สูตรทุกตัว ·
ผังแมพ · `MapDimensions` กับ alias · จุดเกิดอยู่บนพื้น · ยามเวลาวิ่งถึงรังบอสด่าน 1 ≤ 15 วิ ·
ตารางความเร็ว 5 ขั้น · ตัวคูณถดถอยจริง · ความหนากำแพงผูกกับความเร็ว · เพดาน 200 ·
ของในคอกนั่งบนพื้นพอดีทุกขนาด · รัศมีทรงกลม = min(X,Y,Z)/2)

ตัดสินครบแล้ว:
- ✅ สูตร damage และเงิน = รากที่สอง · เงินคูณ ×10 ต่อด่าน
- ✅ `uid` เป็น global string `"<UserId>-<n>"`
- ✅ HP กำแพง/ทหารที่ตีค้างไว้ เซฟลง DataStore (`stageProgress` 9 ช่อง)
- ✅ ระบบรบเป็นแบบปล่อยต่อเนื่อง (Age of War) — ยกเลิก "ส่งทีเดียวแล้วรอผล"
- ✅ ด่าน 1 ไม่มีกำแพง/ทหารฝ่ายรับ
- ✅ อัตราผลิตแปรตามน้ำหนักแม่ `^0.25`
- ✅ **ตัวคูณ damage ซื้อด้วยเงิน** 72 ขั้น ×1.1 · เพดาน `wallProgress × 8`
  (`STAGE_DAMAGE_BASE` ลบแล้ว) · ราคาดูดส่วนเกินจาก 299 เท่าเหลือ 2.5 เท่า
- ✅ เกมเอียงไปทาง idle มากขึ้น = **ตั้งใจ ไม่ใช่ผลข้างเคียงที่ต้องแก้กลับ**
- ✅ ตัดขั้นบนของคอก (15→10) และ upgrade อัตราผลิต (10→5) — เวลาตีไม่เปลี่ยน
- ✅ **ด่าน 9 = 54 ชม. ยอมรับได้** ต้องการให้ใช้เวลานาน กันไปถึงจุดตันเร็วเกินไป
- ✅ เงินบอสแบ่งเท่ากันทุกคนที่ร่วมตี
- ✅ ตัวคูณคลาส 1/6/36/216/432 — คลาสคือแกนไต่ของเกม · SS มีเพดานกัน pay-to-win
- ✅ เงินจากการฆ่าทหาร (×20/ด่าน) + ฆ่าบอส (×10/ด่าน)
- ✅ ตารางสุ่มน้ำหนักเหมือนกันทุกด่าน = **ตัดสินถาวร ไม่ใช่ของค้าง**
- ✅ ยามวัดผู้เล่นที่มีอยู่จริง (แม่ tier 1 คงที่ + คลาสไต่ + เวลาฟาร์ม ≤ 5× เวลาตี)
- ✅ turret เก็บเป็น **สัดส่วน** (`TURRET_TOLL` 10% → 20%) คำนวณ damage เอง
  ไม่ต้องแก้มือเวลาปรับอย่างอื่นอีกแล้ว · มียามกันค่าที่ทำให้เกมตัน
- ✅ cap คลัง 500 ตายตัว ไม่มีระบบอัป
- ✅ บอสแต่ละด่านวางไข่คนละชนิด (`egg_stage1..9`) · ตารางคลาสไล่ตามด่าน
- ✅ น้ำหนักสุ่มตั้งแต่บอสวางไข่ ขนาดไข่บอกน้ำหนัก · ตัวละครสุ่ม**ตอนวางไข่ลงสวนฟัก** (ไม่ใช่ตอนฟักเสร็จ — ย้ายจังหวะเพื่อคำนวณเวลาฟักตามคลาส) แต่ **ยังไม่บอก client จนกว่าจะฟักเสร็จ** ความเซอร์ไพรส์ตอนฟักยังอยู่ครบ
- ✅ **เวลาฟักไข่ตามน้ำหนัก+คลาส** (`Config.getHatchSeconds`) แทนสูตรเดิม (`SECONDS_PER_STAGE × เลขด่าน`) —
  tier น้ำหนัก 1 นาที → 24 ชม. (คลาส C) คูณตัวคูณคลาส C×1 · B×1.15 · A×1.35 · S×2 · SS×2.6
  (สูงสุดจริง tier 7 × SS = 62.4 ชม. เพดาน `MAX_HATCH_SECONDS` 7 วัน) · ไข่ตำนานฟักคงที่ 1 นาทีเสมอ
  (จ่ายเงินจริงซื้อความเร็ว) · ไม่ผูกกับด่านอีกต่อไป จึงไม่ชนกับรอบรีเกิดบอส 5 นาที
  · `EggTypes[eggId].hatchTime` ของไข่รายด่านเหลือเป็น metadata (ไม่ได้ลบ)
- ✅ **ขนาดโมเดลไข่/แม่ scale ตามน้ำหนัก** (`Config.getEggVisualSize` / `getMotherVisualSize`)
  ตัวคูณชุดเดียวกัน tier 1→7 = ×1.00 → ×5.25 (`Config.Balance.VisualScale`) คนละฐาน (ไข่/แม่)
  · แม่ในคอก **1:1 เท่าขนาดถือ/ส่งรบ** (`MOTHER_PEN_SHRINK = 1` — เดิม 1/10 เปลี่ยนเพราะโมเดลจริงเล็กเกินไป) ไข่ไม่มีเวอร์ชันย่อ
- ✅ **อัปเกรดคอก** (`UpgradePenRequest`) ใช้ตาราง `Config.Balance.Pen` ตรง ๆ (Lv1=5→Lv10=14 ตัว
  ราคา 1K→100B) หักเงิน+เพิ่มเลเวลพร้อมกัน ความจุใหม่มีผลทันที (อ่านสดทุกจุด ไม่ cache)
- ✅ **ขายแม่** (`SellMotherRequest`) **เฉพาะแม่ในกระเป๋าเท่านั้น** (แม่ในคอกต้องย้ายออกมาก่อน
  กันขายพลาดตัวที่กำลังผลิต) ราคา = `Config.getMotherSellPrice()` ที่มีอยู่แล้ว (รายได้ 30 นาที
  ไม่ใช่ 60 นาที) คำนวณฝั่ง server ล้วน ๆ ไม่มีพารามิเตอร์ราคาให้ client ส่งมาเอง
- ✅ **ข้อ D (คอก+กระเป๋าเต็มพร้อมกันตอนไข่ครบเวลาฟัก)** แก้โดยไม่แตะ schema เลย — `hatch()`
  ไม่เคลียร์ `hatching` slot ถ้าวางแม่ไม่ได้ (ค้างไว้เฉย ๆ ให้ tick/event ถัดไปลองใหม่เอง)
  ย้ายเข้าอัตโนมัติทันทีที่มีที่ว่างเปิด (ขาย/อัปคอก/ย้ายแม่) เรียงตาม `hatchAt` น้อยสุดก่อน
  · sync ส่ง `stuckHatchCount` + `hatching[i].stuck` ให้ client ใช้แจ้งเตือน (ยังไม่ทำ UI)
- ✅ ตารางสุ่มน้ำหนักใช้ชุดเดียวกันทุกด่าน
- ✅ บัฟสถานะ 4 อย่าง โครงพร้อม ค่ายังไม่เปิด
- ✅ ไข่ซื้อด้วยเงินในเกมไม่ได้ — บอส หรือ Robux เท่านั้น (ตัด `price` ออกแล้ว)
- ✅ ไข่ตำนาน = รับประกัน tier 3 ขึ้นไป · SS ออกจากไข่ตำนานเท่านั้น
- ✅ **ผู้เล่นต่อเซิร์ฟ 7 → 6 คน** ให้ตรงกับคอก 6 แปลง · ไข่/คน/ชม. 8.57 → 10 (+16.7%)
- ✅ **แมพสร้างจากสคริปต์** ไม่ปั้นมือ · ทุกพิกัดอยู่ใน `Config.MapDimensions`
- ✅ **กำแพง/ทหารฝ่ายรับ/กองทัพตัวเอง วาดฝั่ง client** — การชนแยกคนได้ฟรี
  ยอมรับผลข้างเคียงว่าเห็นคนอื่นเดินทะลุกำแพงที่ตัวเองยังพังไม่ได้
- ✅ **แม่เดินไปมาด้วย CFrame lerp ไม่ใช้ Humanoid** · ไม่เซฟตำแหน่ง
- ✅ คอกเป็นพื้นที่เปิดโล่ง ไม่มีช่องตาราง · ไข่วางตรงไหนก็ได้
- ✅ **กันส่งแม่ในคอกไปรบด้วยโครงข้อมูล ไม่ใช่สิ่งกั้นในแมพ** — แม่ในคอกไม่ขึ้นในกระเป๋า
  ลานคอกจึงติดเลนรบได้เลย ไม่ต้องสร้างกำแพงกั้นโซน
- ✅ `MaxPlayers = 6` ใน `default.project.json` + เช็คซ้ำตอนบูต (เว็บ Roblox ทับค่าได้)
- ✅ **แมพเป็นแท่นลอย ไม่มีกำแพงล้อมรอบ** · ขอบกันด้วยกำแพงใส ไม่ใช่ "ตกแล้วเกิดใหม่"
- ✅ **`Config.MapDimensions` เป็นแหล่งความจริงแหล่งเดียว** · `Config.Map` เป็น alias
- ✅ ผู้เล่นวิ่ง 32 (ตั้งที่ `StarterPlayer`) · เกิดใกล้คอกตัวเองด้วย `SpawnLocation` ต่อคอก
- ✅ `WANDER_SPEED` เป็น **stud/วินาที** ไม่ใช่เวลาต่อระยะ — ขยายคอกแล้วแม่ไม่เดินช้าลง
- ✅ **เอาแท่นวาปออกทั้งหมด** เดินไปเองทุกที่ · แก้ปัญหาระยะทางด้วยการซื้อความเร็วแทน
- ✅ **ซื้อความเร็ว 5 ขั้น ×1 → ×4 · ตัวคูณถดถอย** · 5 ขั้นไม่ใช่ 10 เป็นการตัดสินถาวร
- ✅ **ความหนากำแพงผูกกับความเร็วสูงสุดด้วยสูตร** ไม่ใช่เลขตายตัว — ขึ้นความเร็วแล้วลืมเพิ่ม เซิร์ฟไม่บูต
- ✅ **ยอมรับว่าความเร็วไม่ได้แข่งกับ damage** เป็นบ่อดูดส่วนเกินหลัง damage ชนเพดานต่อด่าน
- ✅ **รั้วคอกทะลุได้ (`CanCollide = false`)** ยกเว้นจากกฎความหนา-ความเร็ว · ประตูเว้นช่อง ไม่มีบานเปิด
- ✅ **ทางเดินกลางกว้าง 90 (≥ เลนรบ)** และเป็นหญ้ากลืนกับพื้น ไม่ใช่แถบเทา
- ✅ **`Boundary.Margin` = แถบหญ้านอกกำแพง** พื้นโตตาม ไม่ใช่หดกำแพงเข้ามา
- ✅ **ยุบ `Config.Map` alias ทิ้ง** เหลือชื่อเดียวคือ `Config.MapDimensions`
- ✅ **กระโดดใช้โหมด JumpHeight (7.2)** ไม่ใช่ JumpPower — Config ตรงกับที่ Roblox ใช้จริงแล้ว
- ✅ **ลูกบิดสมดุลทั้งหมดอยู่ใน `Config.Balance`** 17 กลุ่ม · `validate()` กันไม่ให้หลุดออกไปชั้นบน
- ✅ **DataStore ใช้ `UpdateAsync` ทุก path ห้าม `SetAsync`** · session lock 5 นาที ·
  โหลดไม่ได้ = **เตะออก ไม่มีโหมด guest** (เล่นต่อ = autosave เขียนข้อมูลเปล่าทับของจริง)
- ✅ **Studio ใช้ DataStore คนละตัว** เลือกด้วย `RunService:IsStudio()` ไม่ใช่ธงที่ต้องสลับมือ
- ✅ **แจกไข่เริ่มต้นเฉพาะผู้เล่นใหม่จริง** (ธง `isNew` จาก `DataService.loadAsync`)
  ไม่งั้นได้ไข่ฟรีทุกล็อกอิน — บั๊กที่มองไม่เห็นตอนยังไม่มี DataStore เพราะทุกคนเป็นผู้เล่นใหม่ตลอด
- ✅ **`stageProgress[stage].defendersRemaining` เริ่มจาก `Config.getStageDefenderHp(stage)`**
  (HP ทหารฝ่ายรับล้วน ๆ) **ไม่ใช่ `getStageTotalHp()`** ซึ่งเป็นผลรวมทหาร+กำแพง — ใช้ผิดตัวจะเท่ากับ
  ให้กำแพงมี HP ซ้ำสองชั้น (นับใน defendersRemaining ด้วย แล้วก็ยังนับใน wallHpRemaining อีกที)
- ✅ **เงินรางวัลจากการฆ่าผูกกับ "ทหารฝ่ายรับ" เท่านั้น ไม่มีฟังก์ชันรางวัลของกำแพงเลย** — ตีกำแพง
  ไม่ได้เงินสักบาท (`getStageDefenderRewardTotal`/`getDefenderKillReward` ยืนยันแล้ว)
- ✅ **การรบไม่มี offline catch-up** (ตรงข้ามกับ `ProductionService` โดยตั้งใจ) — `CombatService.tick()`
  รับ `elapsedSeconds` ตรง ๆ จากลูปที่เรียก ไม่อ่าน timestamp ย้อนหลัง ปิดเกม/ออกเกม = หยุดคืบหน้าทันที
- ✅ **`releaseOrder`** (อาเรย์ stack key ที่ผู้เล่นจัดลำดับปล่อยเอง) — กองที่หมดยังค้างอยู่ในลำดับ
  ไม่ถูกลบ (เผื่อผลิตเพิ่มมาเติมทีหลัง) · กองใหม่ต่อท้ายอัตโนมัติ · `SetReleaseOrderRequest` เช็ค
  round-trip ผ่าน `Config.makeStackKey()` เป๊ะ ปฏิเสธทั้งชุดเงียบ ๆ ถ้ามี key แปลกปลอมแม้แค่ตัวเดียว
- ✅ **`combatAutoPaused`** (ฟิลด์ใหม่แยกจาก `summonEnabled`) — true เฉพาะตอน auto-pause ปิดปุ่ม
  อัญเชิญให้เอง (ผู้เล่นปิดเองไม่ตั้งค่านี้) ให้ UI แยกแสดงข้อความแจ้งเตือนได้ถูกสาเหตุ
- ✅ **โมเดลทหาร (`TroopRenderer`) เป็นภาพประกอบล้วน ๆ ไม่ผูกกับ damage จริงแบบ 1:1** — สปอน/เดิน
  จำลองจากอัตราปล่อยจริง (`Config.getReleaseRate`) เท่านั้น ประกอบจาก Part (หัว-ลำตัว-แขน-ขา)
  ขยับด้วย `Model:PivotTo()` **ห้ามใช้ Humanoid** (เหตุผลเดียวกับแม่ในคอก) · เดินด้วย**เวลาคงที่**
  (`WALK_SECONDS_TO_WALL` = 30 วิ ค่าเดียวกับที่ `Config.getVisibleUnitCount()` สมมติไว้แล้ว)
  ไม่ใช่ความเร็วคงที่ — ยอมรับว่าความเร็วที่เห็นต่างกันตามด่าน (ด่าน 2 ใกล้กว่าด่าน 9 มาก)
- ✅ **ทหารฝ่ายรับแสดงจำนวนตามสัดส่วน `defendersRemaining`** ผ่าน `Config.getDisplayModelCount()`
  ที่มีอยู่แล้ว รีโพซิชันใหม่ทั้งชุดเฉพาะตอนด่านที่กำลังตีเปลี่ยน ไม่ขยับตัวที่เหลืออยู่แล้วทุก sync
  (กันโมเดล "กระโดด" ตำแหน่งทุก ~1 วิ)
- ⚠️ **ช่องว่างที่ยังไม่ได้เชื่อม**: `data.wallProgress` (ตัวคูณเงิน + เพดาน upgrade damage)
  กับ `data.stageProgress` (ความคืบหน้าการรบจริงจาก `CombatService`) **เป็นคนละตัวกัน ไม่มีอะไร
  เชื่อมสองอันนี้เข้าหากันเลย** — ด่านที่ตีผ่านจริงไม่ได้ทำให้ `wallProgress` ขยับขึ้นอัตโนมัติ
  ต้องตัดสินใจก่อนเริ่ม Phase 3B-2/3C ว่าจะเชื่อมยังไง (แก้ใน `CombatService` ซึ่งเป็นเรื่องตรรกะ
  คำนวณ ไม่ใช่ภาพ/UI)

ค้างอยู่ตอนนี้:
- **ยังไม่ได้ทดสอบใน Studio** → เช็คลิสต์ใน `docs/map-layout.md` §11 และ `docs/phase-1.5-rework.md` §8
- **Phase 3A+3B-1 ยังไม่ได้ทดสอบใน Studio เลย** — ต้องเปิดเกมจริงดูว่ากำแพงหายตามด่านที่ตีผ่าน
  จริง โมเดลทหารสองฝั่ง (เขียว/แดง) วิ่ง/ยืนถูกตำแหน่ง ปุ่มอัญเชิญ+แผงจัดคิวทำงาน (ดูรายการเต็ม
  ที่ต้องเช็คในสรุปท้ายงาน 3B-1)
- **`data.wallProgress` ยังไม่เชื่อมกับ `stageProgress` ที่ผ่านจริง** (ดูรายการ ⚠️ ด้านบน)
  ตัวคูณเงิน/เพดาน upgrade damage จึงยังไม่ขยับตามความคืบหน้าการรบจริง — ต้องตัดสินใจก่อน 3B-2/3C
- **ขนาดแมพขยายรอบที่ 3 แล้ว ยังไม่ได้ลองเดินจริง** — ลาน 382 × 390 · เลน 1,620 ·
  ทั้งแมพ 2,002 × 390 · วิ่งจากคอกถึงรังบอสด่าน 1 = **11.6 วิ** (ยามกำหนดไม่เกิน 15)
  ดูตัวเลขด้วย `luau tools/dump-map.luau` · ตัวที่กระทบมากสุดคือ `MapDimensions.Lane.LengthPerStage`
- ✅ **เพดานไข่ในกระเป๋าขึ้นเป็น 10,000 แล้ว** พร้อมโครงใหม่ `{ nextEggId, items }`
  ⚠️ **แต่ UI ยังไม่ virtualize** — ตอนนี้ server ส่งแค่ 50 ฟองแรกต่อ sync กันถล่มแบนด์วิดท์
  ลิสต์เต็มต้องรอ Phase 5.5 (`docs/hotbar-design.md`)
- ✅ มี DataStore แล้ว (Phase 2A) — แต่ **ยังไม่เคยรันกับ DataStore จริงสักครั้ง**
  ⚠️ `Config.DataStore.MAX_PLAYER_DATA_BYTES` วัดด้วยตัว encode ของเราเอง ไม่ใช่ของ Roblox
  จึงตั้งเพดานไว้ 3 MB ไม่ใช่ 4 MB · เคสเต็มพิกัดตอนนี้ = 708 KB (16.9% ของลิมิตจริง)
- ⚠️⚠️ **TEMP: `Config.Balance.NewPlayer.startingEggs.egg_stage1` ตั้งไว้ที่ 100 ชั่วคราว**
  (ปกติคือ 1) เอาไว้ทดสอบ Phase 2B บนเกมจริงให้มีแม่/ไข่กระจายครบทุก tier โดยไม่ต้องรอบอส
  **ต้อง revert กลับเป็น 1 ก่อน publish จริง (Phase 7)** — comment เตือนไว้ที่ต้นทางใน
  `Config.lua` แล้ว (ตรง `local startingEggs`) และเทสต์ `tests/config.spec.luau` ก็ยึดค่า 100
  นี้ไว้ชั่วคราวเช่นกัน (ต้องแก้กลับเป็น 1 พร้อมกันตอน revert)
- รายการที่เคลียร์ตอนถึงเฟสนั้นได้ อยู่ใน `docs/data-schema.md` §13
